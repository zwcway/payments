<?php

return array (

    /*
    |--------------------------------------------------------------------------
    | 服务器异步通知页面路径
    |--------------------------------------------------------------------------
    |
    | 需http://格式的完整路径，不允许加?id=123这类自定义参数
    |
    */
    'notify_url' => '',

    /*
    |--------------------------------------------------------------------------
    | 页面跳转同步通知页面路径
    |--------------------------------------------------------------------------
    |
    | 需http://格式的完整路径，不允许加?id=123这类自定义参数
    |
    */
    'callback_url' => '',

    /*
    |--------------------------------------------------------------------------
    | 操作中断返回地址
    |--------------------------------------------------------------------------
    |
    | 用户付款中途退出返回商户的地址。
    | 需http://格式的完整路径，不允许加?id=123这类自定义参数
    |
    */
    'merchant_url' => '',

    /*
    |--------------------------------------------------------------------------
    | 签名方式
    |--------------------------------------------------------------------------
    |
    | 可能的值有：MD5、DSA、RSA/0001
    |
    */
    'sign_type' => 'MD5',

    /*
    |--------------------------------------------------------------------------
    | ca证书路径地址
    |--------------------------------------------------------------------------
    |
    | 用于curl中ssl校验.
    | 证书需放在 app/storage/cert/ 文件夹下
    |
    */
    'cacert' => app('path') . '/storage/cert/caceat.pem' ,
    'account' => '1001',//商户账号
    'key' => 'test', //商户密钥
    'reurl' => 'http://www.domain/payments/respond' //支付返回地址
);
